#labels Featured
= Downloading =

Download the single file OpenXION-1.1-dist.zip. Unzip to the directory of your choice.

= Using =

You can use OpenXION without installing it. Just cd to the directory where the archive was unzipped, then run the xion command:

{{{
./xion
}}}

To get a list of command line options, run the xion command with the --help option:

{{{
./xion --help
}}}

= Installation =

To give OpenXION a more permanent home on your system, simply run the install script:

{{{
sudo ./install
}}}

The jars will be placed in /usr/lib, and the three commands xion, xiondoc, and xe will be placed in /usr/bin.

= Installation from Source =

Use this command to check out the latest project source:

{{{
svn checkout http://openxion.googlecode.com/svn/trunk/main/java/OpenXION/ openxion
}}}

Change to the directory you just checked out:

{{{
cd openxion
}}}

Build and install with make:

{{{
make
sudo make install
}}}

Enter your administrator password when prompted.